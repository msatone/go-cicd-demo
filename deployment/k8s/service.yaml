apiVersion: v1
kind: Service
metadata:
  name: my-go-app-service
  namespace: my-app-ns
  labels:
    app: my-go-app
    managed-by: harness
spec:
  type: LoadBalancer
  selector:
    app: my-go-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
